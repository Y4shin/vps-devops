---
# Runs the re-runnable host base config plus the service deploys.
# Use this after the one-time root bootstrap has already created the deploy user.
- import_playbook: base.yml
- import_playbook: site.yml
