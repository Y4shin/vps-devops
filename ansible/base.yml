---
# Base host configuration after the initial bootstrap. Connects as the deploy
# user and escalates to root via `su`, so it remains usable even after root SSH
# login has been disabled.
- name: Configure VPS Base
  hosts: vps
  gather_facts: true
  become: true
  roles:
    - base
